info:
  name: Get LAB by ID
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}/api/lab/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
